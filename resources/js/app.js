import './bootstrap';

// 1. IMPORT BOOTSTRAP: This is the critical line that adds the JS functionality 
//    for the navbar toggler and the dropdown menu.
import 'bootstrap'; 

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();